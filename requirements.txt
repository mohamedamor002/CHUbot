# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.18          # file uploads (HR docs)

# ── Data Validation ───────────────────────────────────────────────────────────
pydantic>=2.10.0
pydantic-settings>=2.7.0

# ── LLM & RAG Orchestration ───────────────────────────────────────────────────
langchain>=0.3.0
langchain-community>=0.3.0
langchain-classic>=0.1.0          # EnsembleRetriever (hybrid BM25 + vecteur)
langchain-ollama>=1.1.0           # intégration Ollama (modèles locaux)
langchain-chroma>=0.1.0           # intégration ChromaDB pour LangChain
langchain-text-splitters>=0.3.0   # découpage de documents

# ── Vector Store ──────────────────────────────────────────────────────────────
chromadb>=0.6.0                   # local vector DB

# ── Document Loaders ──────────────────────────────────────────────────────────
pymupdf>=1.24.0                   # PDF (fiche de poste, contrats, règlements…)
python-docx>=1.1.0                # Word documents
docx2txt>=0.8                     # requis par Docx2txtLoader de LangChain
openpyxl>=3.1.0                   # Excel .xlsx et .xlsm (tableaux RH, plannings)
xlrd>=2.0.0                       # Excel .xls (ancien format)
unstructured[xlsx]>=0.10.0        # requis par UnstructuredExcelLoader

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.0
alembic>=1.14.0

# ── Auth & Security ───────────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0  # JWT tokens
passlib[bcrypt]>=1.7.4            # password hashing
python-dotenv>=1.0.0

# ── Web Scraping ──────────────────────────────────────────────────────────────
trafilatura>=2.0.0                # extraction intelligente du contenu principal
playwright>=1.50.0                # navigateur headless pour pages JavaScript
beautifulsoup4>=4.12.0            # parsing HTML (titres de pages, liens)
lxml>=5.0.0                       # parser HTML rapide (requis par BeautifulSoup)
requests>=2.32.0                  # requêtes HTTP pour scraping et téléchargements

# ── HTTP Client ───────────────────────────────────────────────────────────────
httpx>=0.28.0

# ── Hybrid Search & Reranking ─────────────────────────────────────────────────
rank-bm25>=0.2.2                  # BM25 lexical retrieval pour hybrid search
flashrank>=0.2.9                  # cross-encoder léger pour reranking (sans GPU)

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.25.0
pytest-cov>=6.0.0

# ── PostgreSQL ────────────────────────────────────────────────────────────────
asyncpg>=0.30.0                   # PostgreSQL async driver
# redis>=5.2.0
# hiredis>=3.0.0
# celery[redis]>=5.4.0
# faiss-cpu>=1.12.0
# unstructured[local-inference]>=0.16.0